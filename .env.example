# SMTP configuration (never commit real secrets)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_USE_TLS=true

# Email addresses
SMTP_FROM_EMAIL=alerts@example.com
ALERT_TO_EMAIL=ops@example.com
